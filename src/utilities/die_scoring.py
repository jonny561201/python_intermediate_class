def score_ones(list_die):
    score = 0
    for die in list_die:
        if die == 1:
            score += die


def score_twos(list_die):
    score = 0
    for die in list_die:
        if die == 2:
            score += die


def score_threes(list_die):
    score = 0
    for die in list_die:
        if die == 2:
            score += die


